apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jaar
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    # Git-sourced: works on any cluster, no minikube mount required.
    # Use jaar-app-local.yaml for the mount-based local inner loop.
    repoURL: https://github.com/alchemesh-io/j.a.r.v.i.s.git
    path: helm/jaar
    targetRevision: main
    helm:
      releaseName: jaar
  destination:
    server: https://kubernetes.default.svc
    namespace: jaar
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
