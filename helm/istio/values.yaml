# Istio sub-chart values

base:
  defaultRevision: default

istiod:
  pilot:
    resources:
      requests:
        cpu: 100m
        memory: 128Mi

gateway:
  name: istio-ingressgateway
  service:
    type: LoadBalancer
